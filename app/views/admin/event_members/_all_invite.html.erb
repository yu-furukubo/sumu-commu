<% if residence_members.count != event.event_members.count %>
  <%= link_to "全員招待する", admin_residence_event_event_members_invite_all_path(params[:residence_id], event), method: :patch, remote: :true, class: "btn btn-primary" %>
<% end %>