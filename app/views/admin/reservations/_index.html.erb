<table class='table table-borderless'>
  <thead>
    <tr>
      <% if controller.controller_name == "reservations" %>
        <% if controller.action_name != "residence_search" %>
          <th>居住地</th>
        <% end %>
        <th>予約品・設備</th>
      <% end %>
      <th>日程</th>
      <th>予約者名</th>
    </tr>
  </thead>
  <tbody>
    <% reservations.each do |reservation| %>
      <tr>
        <% if controller.controller_name == "reservations" %>
          <% if controller.action_name != "residence_search" %>
            <td><%= reservation.residence.name %></td>
          <% end %>
          <% if reservation.equipment_id.present? %>
            <td><%= link_to reservation.equipment.name, admin_equipment_path(reservation.equipment) %></td>
          <% elsif reservation.facility_id.present? %>
            <td><%= link_to reservation.facility.name, admin_facility_path(reservation.facility)%></td>
          <% end %>
        <% end %>
        <td>
          <%= link_to admin_reservation_path(reservation) do %>
            <% if reservation.started_at.to_date == reservation.finished_at.to_date %>
              <%= l reservation.started_at %> ~ <%= l reservation.finished_at.to_time, format: :short %>
            <% else %>
              <%= l reservation.started_at %> ~ <%= l reservation.finished_at %>
            <% end %>
          <% end %>
        </td>
        <% if reservation.member_id == 0 %>
          <td>管理人</td>
        <% else %>
          <td><%= reservation.member.name %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>