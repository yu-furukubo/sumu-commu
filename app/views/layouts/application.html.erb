<!DOCTYPE html>
<html>
  <head>
    <title>SumuCommu</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/02d3b10da1.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <header style="background-color: rgb(160, 206, 223)">
      <div class="container-fluid">
        <div class="row">
          <div class="pr-5">
            <div class="logo">
              <%= link_to root_path, class: "lead" do %>
                <%= image_tag 'sumu-commu_logo.png', :style => "max-height:120px" %>
              <% end %>
            </div>
          </div>
          <div class="my-auto">
            <div class="row">
              <% if member_signed_in? %>
                <div class="text-top text-dark mx-5">
                  <%= image_tag current_member.get_profile_image, width: '50px' %>
                  <%= current_member.name %>
                </div>
                <div class="d-flex align-items-center">
                  <%= link_to public_search_path do %>
                    <i class="fa-solid fa-magnifying-glass"></i> 検索
                  <% end %>
                </div>
              <% elsif admin_signed_in? %>
                <div class="mx-5">
                  <%= link_to admin_search_path do %>
                    <i class="fa-solid fa-magnifying-glass"></i> 検索
                  <% end %>
                </div>
              <% end %>
            </div>
            <div class="mx-auto">
              <% if admin_signed_in? %>
                <%= render 'layouts/admin_header' %>
              <% else %>
                <%= render 'layouts/member_header' %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </header>

    <main>
      <div class="col-md-6 mx-auto mt-3">
        <div class="text-center text-success font-weight-bold">
          <%= flash[:notice] %>
        </div>
      </div>
      <%= yield %>
    </main>

    <footer class="text-center text-light mt-auto" style="background-color: rgb(160, 206, 223)">
      <div class="m-3">
        <p>CopyRight すむこみゅ<SUMU-COMMU></p>
      </div>
    </footer>
  </body>
</html>
