<% if Read.find_by(board_id: board.id , member_id: current_member.id).present? %>
  <%= link_to "未読に戻す", board_reads_path(board), method: :delete, remote: true, class: "btn btn-secondary" %>
<% else %>
  <%= link_to "既読にする", board_reads_path(board), method: :post, remote: true, class: "btn btn-success" %>
<% end %>