<% if community.community_members.where(member_id: current_member.id).present? %>
  <%= link_to "退会する", community_community_member_quit_path(community), method: :delete, remote: :true, class: "btn btn-secondary" %>
<% else %>
  <%= link_to "入会する", community_community_members_path(community), method: :post, remote: :true, class: "btn btn-success" %>
<% end %>